# Unpinned (with safe floors) so the host installs prebuilt wheels matching its
# own Python version. Streamlit Cloud currently runs Python 3.14, which has no
# wheels for older pinned numpy/scipy — letting the resolver pick the latest
# compatible versions avoids a slow, failure-prone source build.
streamlit>=1.40
numpy>=2.1
scipy>=1.14
pandas>=2.2
plotly>=5.22
matplotlib>=3.8
yfinance
